# Base URL for Orbit API; default resolves to current origin when served from the same container.
VITE_API_BASE_URL=http://localhost:8081

# Output base path for built assets (defaults to /ui/).
VITE_APP_BASE=/ui/
